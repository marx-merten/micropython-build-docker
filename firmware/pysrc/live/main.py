# This is the main file that is run when the device boots up.
# It is responsible for setting up the device and running the main loop.
#

# Default Logging
import ulogging as logging
LOG = logging.getLogger(__name__)

LOG.info("STARTING MAIN")
